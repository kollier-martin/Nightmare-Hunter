using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mushroom : EnemyP
{
    // Start is called before the first frame update
    void Start()
    {
        SetVariables(7, 100, 10, 1, 1);
    }
}
